# Independent oracle lane

This directory is reserved for scripts that compare package public APIs with
independent definitions or other packages' public APIs. Oracle code must not be
imported by `bayesmokken` and must not duplicate production helpers.

Phase 5 uses frozen hand calculations only. Random `mokken::coefH()` and
empirical-golden comparisons begin in Phase 6.
